#include <stdio.h>

/**
 * add - adds numbers
 * @a: an operand
 * @b: an operand
 *
 * Description: this adds two ints
 *
 * Return: returns int
 */

int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
