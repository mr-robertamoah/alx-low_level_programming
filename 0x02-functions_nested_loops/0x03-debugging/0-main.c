#include "main.h"

/**
 * main - Entry point
 *
 * Description: prints output of positive_or_negative to
 * test if it prints the right things
 *
 * Return: returns 0 (Success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
